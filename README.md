# SortMaster-Pro-By-Vivek
The core idea was to build a wesbite where the internal state of an algorithm is mapped directly to a visual coordinate system. Each bar represents a data point, and its height represents its value. As the algorithm iterates, the website captures the "thinking process" of the computer.
